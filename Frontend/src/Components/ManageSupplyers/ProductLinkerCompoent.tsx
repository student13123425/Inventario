import React, { useState } from 'react'

export default function ProductLinkerCompoent(props:{}) {
    const [IsNewLink,setIsNewLink]=useState<boolean>(false);
  return (
    <div>
        
    </div>
  )
}
